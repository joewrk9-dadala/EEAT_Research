# 新增檔案的用途與設計邏輯

這些檔案是執行 `prompt1～8` 後留下的成果與交接資料。原始 prompt 定義「要做什麼」，新增檔案記錄「這次實際判斷了什麼、改了什麼，以及下一階段要接收什麼」。

你提到的 `01_rule.md`，實際檔名是 **`01_rules.md`**。以下路徑除特別註明，均位於 `audit_run_2026-09-08/`。

## 1. 為什麼不只交一篇修訂稿？

只看修訂稿，可以知道最後寫成什麼樣子，卻不容易回答：哪句有問題？違反哪項規則？根據什麼修改？修改後是否真的解決？

因此，本次把你的 prompt 所要求的規則表、主張表、問題表、證據表和複審結果保存下來，形成這條可回查的關係：

**原文段落 → 主張 → 審核規則與問題 → 證據 → 修改 → 複審結果。**

這是檔案拆分的設計依據，不代表八個階段都必須產生同樣數量的檔案。你的 prompt 要求這些交付內容，但沒有指定必須使用這些檔名，或必須同時存 Markdown 與 JSON；檔案格式與拆分方式是我這次的實作選擇。

## 2. 檔名怎麼看？

| 命名 | 意義 |
| --- | --- |
| `01_`～`08_` | 對應產出該檔案的流程階段，不是重要程度或文章版本。 |
| `V0` | 原稿版本。 |
| `V1` | 第一版修訂稿。 |
| `.md` | 給人閱讀，適合正文、說明、表格和審核報告。 |
| `.json` | 把資料存成固定欄位，方便查找 ID、比對版本或交給後續程式使用。 |

例如 `02_claims_V0.md` 和 `.json` 表達的是同一份原稿主張清單，分別方便閱讀與資料處理，並不是兩次獨立審核。部分 JSON 額外保存完整原文、歷史狀態或欄位，因此兩種格式不一定逐字相同。

目前沒有建立兩種格式的自動同步機制。如果日後直接修改其中一份，需要同步更新另一份，避免內容不一致。

## 3. 流程 1～8 的檔案，各自解決什麼問題？

| 階段與檔案 | 新增原因 | 如何交給下一步 |
| --- | --- | --- |
| **1：`01_rules.md`** | 把 `common`、原始寫作要求與證據邊界轉成可判定的規則，固定嚴重度、字數口徑、驗收與停止條件。避免改稿後才調低標準。 | 初審、修訂與複審都使用這份規則。它是 `prompt1` 的產出，不是取代 `common`。 |
| **2：`02_numbered_V0.md`** | 保存完整原文並加入段落 ID，讓問題能定位到具體段落。 | 初審指出問題位置，修訂時回查原句。 |
| **2：`02_claims_V0.md`／`.json`** | 把段落拆成可判定的主張，區分模擬、建議、評論和待查事實。同一段可能包含多項不同性質的主張。 | 初審與查證可針對單項主張判斷，不必把整段一律視為有錯。 |
| **2：`02_evidence_map.md`** | 盤點主張目前對應哪些資料，以及哪些部分仍無支持。這時只是盤點，不能視為已完成查證。 | 流程 3、4據此安排查核優先順序。 |
| **3：`03_initial_review.md`** | 彙整初審結論、意圖完成度、問題與必須保留的有效內容。 | 告訴查證者要確認什麼，也提醒編輯不要刪掉原稿的好內容。 |
| **3：`03_issues_initial.json`** | 為每個問題保存 ID、位置、規則、嚴重度與可驗收的修正要求。 | 修訂與複審沿用同一問題 ID，追蹤是否處理。 |
| **4：`04_source_review.md`** | 記錄實際讀取的來源、支持範圍、主張判定和資料缺口。 | 告訴編輯哪些表述能保留、哪些需要限定或刪除。 |
| **4：`04_evidence.json`** | 集中保存證據 ID、網址或檔案位置、日期、原文定位與限制。 | 其他表格引用證據 ID，就能回到同一筆來源資料。 |
| **5：`05_revision.md`、`05_changes.json`** | 記錄改了哪裡、對應哪個問題、為什麼這樣改。修訂者此時只標示「已修訂待複審」。 | 流程 6核對修正要求，不能只憑編輯說已改善就結案。 |
| **5：`05_numbered_V1.md`、`05_paragraphs_V1.json`** | 保存修訂後的段落位置，以及新舊段落對照。 | 複審可直接找到改後文字，保留既有段落的追蹤關係。 |
| **5：`05_claims_V1.md`／`.json`** | 更新修改後的主張與證據關係，保留原主張，登錄新增主張。 | 複審除了查舊問題，也檢查改稿是否新增無依據內容。 |
| **6：`06_review.md`** | 重新核對全文、問題與固定驗收條件，決定通過、退回或停止待查核。 | 提供最終文章驗收結論，供流程 8報告引用。 |
| **6：`06_issues_final.json`** | 保存問題最後狀態與初審→修訂→複審的狀態歷史。 | 可查詢哪項已解決、哪項仍未解決，不覆蓋初審紀錄。 |
| **7：`07_evaluation_design.md`** | 設計如何比較原稿、一次性改寫與提示詞鏈，區分單篇修改成果和整套流程效果。 | 為未來實驗提供方案。本次沒有實際比較分數。 |
| **8：`08_report.md`** | 把實際紀錄整理成修改前後對照、成果與限制，以及面試說明。 | 給需要快速理解成果的人閱讀，不新增未執行的成果宣稱。 |

另有 `article_V0.md` 和 `article_V1.md`，分別保存乾淨的原稿與修訂稿。正文不插審核 ID，方便直接閱讀；需要定位時才看 `numbered` 編號稿。

## 4. ID 為什麼要分成五種？

| ID | 代表什麼 | 用途 |
| --- | --- | --- |
| `R001…` | 規則 | 說明判斷標準。 |
| `P001…` | 段落 | 找到文章位置。 |
| `C001…` | 主張 | 指出段落裡具體在說什麼。 |
| `I001…` | 問題 | 追蹤需要處理的缺失。 |
| `E001…` | 證據或來源資料 | 回查判斷依據及其限制；有 ID 不代表已證實。 |

本次的實際例子：`P016` 原本寫「截圖也只證明截取當下可見的內容」。初審依 `R004` 的證據邊界規則建立 `I002`，參照 `E001` 的限制，改成「協助查看畫面」，並補上來源、編輯與時間對應條件。流程 5留下修改前後文字，流程 6確認符合修正要求後，才把 `I002` 標為已解決。

這樣可以區分「文章在哪裡」「哪個說法」「有什麼問題」與「用什麼資料支持」。同一問題也可能跨多個段落，例如本次介面效果措辭的 `I003`。

注意：`boundary` 原有的 `E01～E05` 是「缺少哪些資料」的分類；新增的 `E001～E004` 是本次來源登錄，兩者不同。內部情境稿即使登錄為證據 ID，也仍然只是模擬設計依據。

## 5. `evaluation/` 為什麼另外拆開？

這個資料夾用於未來驗證「提示詞鏈是否有效」，與本次文章修訂紀錄分開，避免把測試設計誤認成已執行結果。

| 檔案 | 原因與用途 |
| --- | --- |
| `development_inputs.json` | 開發案例，可用於調整 prompt。 |
| `holdout_candidate_inputs.json` | 保留測試候選，正式確認後不應用來調整 prompt。目前還不是已完成的盲測。 |
| `labels_human_only.json` | 候選答案、嚴重度與修法，全部待人工確認；正式測試時不能交給受測模型。單獨存檔只是方便隔離，並沒有實作存取權限。 |
| `fixtures.json` | 明示虛構的測試文件與交易資料，用來測試特定錯誤，不冒充真實證據。 |
| `fixed_evidence_packet.json` | 固定證據包草案，讓比較組取得相同資料，降低來源差異對結果的影響。 |
| `baseline_B_template.md` | 一次性改寫的提示詞模板，作為比較基準。 |
| `baseline_B_article_ready.md` | 已填入本篇文章與背景資料的基準提示詞，方便之後執行；本次尚未送出執行。 |
| `human_scoring_template.csv` | 人工評分欄位，記錄檢出、誤報、修正與新增錯誤。 |
| `run_record_template.json` | 未來記錄模型、輸入輸出、時間、token與成本的空白格式，不是實際執行紀錄。 |

## 6. 其他輔助檔案為什麼存在？

| 檔案或資料夾 | 原因與限制 |
| --- | --- |
| `inputs/` | 保存本次原始輸入快照，日後即使修改原始 prompt，也能知道這次用的是哪一版。 |
| `stage_inputs/stage1.md`～`stage8.md` | 每份放入完整 `common`、當前 prompt及交接檔案位置，方便後續重跑。引用的資料仍要實際讀取；這些檔案不是八次獨立 API 請求的 log。 |
| `manifest.json` | 記錄本次輸入檔雜湊、版本、輪次與執行範圍。雜湊可用來檢查檔案有沒有改變，不能證明內容正確。 |
| `validation.json` | 保存字數、ID唯一性、資料對應、原檔未變與算式檢查。這些是程式檢查，不代替語義審核。 |
| `build_records.py`、`stage45.py`、`stage678.py` | 本次把已整理的內容寫成檔案的腳本。沒有呼叫模型執行審核；重跑只會重建既定內容，不會重新查證或複審。 |
| 執行目錄的 `README.md` | 完整成果索引，方便查詳細資料。 |
| 根目錄的 `README.md` | 給你快速閱讀的精簡摘要。 |

## 7. 實際閱讀需要看哪些？

一般只需要三份：

1. [根目錄 README](README.md)：了解結論。
2. [article_V1.md](audit_run_2026-09-08/article_V1.md)：閱讀修訂後文章。
3. [08_report.md](audit_run_2026-09-08/08_report.md)：了解改了什麼與限制。

需要追查某項判斷時，再沿著問題 ID 查看初審、證據、修改和複審。需要程式整合或大量處理時，再使用 JSON。

這次保留較多檔案，是為了完整對應你提供的八階段交付要求。若日後只需要人工操作，可以把各階段的 Markdown 合併成一份審核報告；若要自動化，則可集中維護一份結構化資料，再產生閱讀報告。無論採哪種形式，重要的是保留規則、問題、證據、版本與驗收之間的關係。
