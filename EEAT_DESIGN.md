# 提示詞如何落實 E-E-A-T

這套設計把 E-E-A-T 轉成具體檢查條件，再透過「審核 → 查證 → 修訂 → 複審」降低不符合原則的產出。單靠提示詞不能保證內容正確，因此必須保留證據限制與未通過的處理方式。

## 1. 每個階段都套用共同限制

每次輸入包含 `common`、當前 `prompt` 與必要的前序成果。`boundary` 限制可下的結論，`user_story` 只提供模擬素材，避免後續改寫把虛構情境變成真實經驗。

## 2. 將 E-E-A-T 轉成可驗收的要求

| 原則 | 提示詞設計 | 檢查重點 |
| --- | --- | --- |
| **Experience（經驗）** | 要求敘事前揭露模擬性質，禁止虛構親身操作、測試或客服回覆。 | 第一人稱是否始終指向模擬角色，而非暗示作者實測。 |
| **Expertise（專業性）** | 將段落拆成可判定主張，分開畫面、操作受理、遊戲結果與帳務；檢查公式及適用前提。 | 是否從單一現象跳到因果結論，或把示例規則當成通用規則。 |
| **Authoritativeness（權威性）** | 要求實際讀取來源、記錄出處與適用範圍；作者資歷、審閱與外部認可須有資料支持。 | 引用是否真正支持該主張，而非只增加引用數量或專業頭銜。 |
| **Trustworthiness（可信度）** | 區分已知、假設與未知，揭露資料缺口；不保證財務結果，不引導公開敏感資訊。 | 結論是否超出證據，平台自述是否被誤當獨立驗證。 |

以上是依本專案需求制定的驗收規則，參考 [Google 官方內容品質指引](https://developers.google.com/search/docs/fundamentals/creating-helpful-content?hl=zh-tw)，不是 Google 認證標準或排名分數。

## 3. 用可追蹤的交接與複審控制品質

`prompt1` 固定規則；`prompt2～4` 建立主張、問題與證據對照；`prompt5` 依問題修訂，狀態只能先標「已修訂待複審」；`prompt6` 重新核對全文與證據後，才判定是否解決。

規則、段落、主張、問題與證據各有固定 ID，讓每次修改都能回查原因。需補證據就退回查證，文字未修好就退回修訂；最多三輪，仍不符要求則交付待查核稿，不放寬標準。

`prompt7～8` 另外限制成果宣稱：沒有比較實驗或人工評分，就不能宣稱流程有效；沒有量測，就不能宣稱排名改善。本次採同一模型分角色複審，不能取代獨立人工審查，也不能透過改寫文字建立真實資歷或外部聲譽。
